{
  "global": [
    {
      "name": "context7",
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Library/API docs lookup — resolves up-to-date docs for any npm package or framework"
    }
  ],
  "profiles": {
    "shopify-liquid": [
      {
        "name": "shopify-dev-mcp",
        "command": "npx",
        "args": ["-y", "@shopify/dev-mcp"],
        "description": "Shopify Liquid/GraphQL schema reference — official MCP for theme and storefront dev"
      }
    ]
  }
}
